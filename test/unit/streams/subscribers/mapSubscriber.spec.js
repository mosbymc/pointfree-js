import { mapSubscriber } from '../../../../src/streams/subscribers/mapSubscriber';
import { subscriber } from '../../../../src/streams/subscribers/subscriber';

describe('Test mapSubscriber', function _testMapSubscriber() {
    it('should invoke the subscriber\'s onNext handler when a successfully transformed value is returned', function _testOnNext() {
        var ms = Object.create(mapSubscriber),
            s = Object.create(subscriber);
    });
});